import { NewlineReplaceBrPipe } from './newline-replace-br.pipe';

describe('NewlineReplaceBrPipe', () => {
  it('create an instance', () => {
    const pipe = new NewlineReplaceBrPipe();
    expect(pipe).toBeTruthy();
  });
});
